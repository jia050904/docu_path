import {useNavigate} from 'react-router-dom';import {useUserData} from '../hooks/useUserData';
export function SuggestedKeywords(){const nav=useNavigate();const [,setRecent]=useUserData<string[]>('recent',[]);return <div className="keyword-row" aria-label="추천 검색어">{['월세 지원','전월세 신고','3.3','근로장려금'].map(keyword=><button key={keyword} onClick={()=>{setRecent(old=>[keyword,...old.filter(item=>item!==keyword)].slice(0,5));nav(`/search?q=${encodeURIComponent(keyword)}`)}}>{keyword}</button>)}</div>}
