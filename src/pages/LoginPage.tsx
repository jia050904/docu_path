import {useState,type FormEvent} from 'react';import {useNavigate,Link} from 'react-router-dom';import {LockKeyhole,Mail,ArrowLeft} from 'lucide-react';import {useAuth} from '../hooks/useAuth';
export function LoginPage(){const {login}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('demo@seoryugil.kr');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();if(login(email,password))nav('/');else setError('이메일 또는 비밀번호를 다시 확인해 주세요.')};return <div className="login-page"><div className="login-panel"><Link to="/" className="back-link"><ArrowLeft size={18}/>홈으로 돌아가기</Link><div className="login-title"><span className="login-icon"><LockKeyhole/></span><p className="eyebrow">COURSE MVP</p><h1>Demo 계정으로<br/>준비 과정을 저장해요</h1><p>실제 인증이 아닌 수업 시연용 로컬 로그인입니다.</p></div><form onSubmit={submit} className="login-form"><label>이메일<div className="input-with-icon"><Mail/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" required/></div></label><label>비밀번호<div className="input-with-icon"><LockKeyhole/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" placeholder="demo1234" required/></div></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="primary-button" type="submit">로그인</button></form><div className="demo-account"><strong>Demo 계정</strong><span>demo@seoryugil.kr</span><span>demo1234</span></div></div><div className="login-visual" aria-hidden><div><span>01</span><p>일상 언어로 검색</p></div><div><span>02</span><p>필요한 서류 확인</p></div><div><span>03</span><p>준비 상태 저장</p></div></div></div>}
