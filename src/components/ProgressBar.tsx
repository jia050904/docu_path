export function ProgressBar({done,total}:{done:number;total:number}){const p=total?Math.round(done/total*100):0;return <div className="progress-block"><div className="progress-label"><span>{done} / {total} 준비 완료</span><strong>{p}%</strong></div><div className="progress-track" role="progressbar" aria-label="준비 진행률" aria-valuemin={0} aria-valuemax={100} aria-valuenow={p}><span style={{width:`${p}%`}}/></div></div>}
